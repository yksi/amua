%h1.text-center.text-success Users list
.row
  .col-md-8.col-md-offset-2
    %ul.media-list
      - @users.each do |user|
        %li.media
          = link_to show_user_path(user), class: 'pull-left' do
            = image_tag 'udef.png'
          .media-body
            %h4.media-heading= user.fullname
            = user.email