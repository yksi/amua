- if user_signed_in?
  = form_for Post.new, remote: true do |f|
    = f.text_area :content, class: 'form-control'
    = f.hidden_field :owner_id, value: @user.id
    %br
    .text-right= f.submit 'Post', class: 'btn btn-lg btn-default'